# CI Main Workflow - Comprehensive Testing
#
# This is the main CI workflow that runs on pushes to main branch.
# It provides validation on the most important platform (Ubuntu x64).
#
# For fast PR validation, use pr-validation.yml instead.

name: CI Main

on:
  push:
    branches: [main, feature/multi-format-serialization-fuse]
    paths-ignore:
      - '.benchmark/**'
      - '.clang-format'
      - '.clang-tidy'
      - '.gitignore'
      - '.maintainer-scripts/**'
      - 'CHANGES.md'
      - 'CITATION.cff'
      - 'TODO'
      - 'doc/**'
      # Note: Allow .github/**/*.md changes to trigger CI for documentation updates
      - 'README.md'
      - 'ARCHITECTURE.md'
      - 'BUILD_SYSTEM_ARCHITECTURE.md'
      - 'CLEANUP_PLAN.md'
      - 'STATUS_REPORT.md'
      - 'WORKFLOW_GUIDE.md'
  workflow_dispatch:
    inputs:
      config:
        description: 'Configuration to test'
        required: false
        type: choice
        options:
          - flatbuffers
          - flatbuffers+modern-thrift
        default: flatbuffers

concurrency:
  group: ci-main-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  # Ubuntu x64 - flatbuffers (PRIMARY TEST)
  test-ubuntu-flatbuffers:
    uses: ./.github/workflows/_build-test-reusable.yml
    with:
      runner: ubuntu-latest
      triplet: x64-linux
      config: flatbuffers
      build-type: Release
      cache-key: ci-main-ubuntu-x64

  # Ubuntu x64 - flatbuffers+modern-thrift
  test-ubuntu-modern-thrift:
    uses: ./.github/workflows/_build-test-reusable.yml
    with:
      runner: ubuntu-latest
      triplet: x64-linux
      config: flatbuffers+modern-thrift
      build-type: Release
      cache-key: ci-main-ubuntu-x64

  # Summary job
  summary:
    name: CI Summary
    needs: [test-ubuntu-flatbuffers, test-ubuntu-modern-thrift]
    runs-on: ubuntu-latest
    if: always()

    steps:
      - name: Display summary
        run: |
          echo "=== CI Main Workflow Summary ==="
          echo ""
          echo "Date: $(date -u +"%Y-%m-%d %H:%M:%S UTC")"
          echo "Run: ${{ github.run_number }}"
          echo "Commit: ${{ github.sha }}"
          echo "Branch: ${{ github.ref_name }}"
          echo ""
          echo "Job Status:"
          echo "  Ubuntu flatbuffers: ${{ needs.test-ubuntu-flatbuffers.result }}"
          echo "  Ubuntu flatbuffers+modern-thrift: ${{ needs.test-ubuntu-modern-thrift.result }}"
          echo ""
          echo "View results in the Actions tab for detailed logs."
