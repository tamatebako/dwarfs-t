# CI Matrix - Comprehensive Testing Across All Platforms
#
# This workflow runs comprehensive tests across ALL supported triplets.
# It provides maximum coverage but takes longer to complete.
#
# IMPORTANT: This is a HEAVY workflow. Use pr-validation.yml or ci-main.yml
# for faster feedback during development.
#
# Estimated Runtime: 2-4 hours depending on GitHub Actions queue

name: CI Matrix

on:
  push:
    branches: [main]
    paths-ignore:
      - '.benchmark/**'
      - '.clang-format'
      - '.clang-tidy'
      - '.gitignore'
      - '.maintainer-scripts/**'
      - 'CHANGES.md'
      - 'CITATION.cff'
      - 'doc/**'
      # Keep markdown docs that might affect CI
      # - '*.md'
  workflow_dispatch:

concurrency:
  group: ci-matrix-${{ github.ref }}
  cancel-in-progress: false  # Don't cancel matrix jobs

permissions:
  contents: read

jobs:
  # ==========================================================================
  # Primary Tests (High Priority)
  # ==========================================================================

  # Linux x64 - Production (FlatBuffers + Legacy Thrift)
  test-linux-x64-production:
    name: Linux x64 - Production
    uses: ./.github/workflows/_build-test-reusable.yml
    with:
      runner: ubuntu-latest
      triplet: x64-linux
      config: production
      build-type: Release
      cache-key: ci-matrix-linux-x64-prod

  # Linux x64 - Experimental (+ Modern Thrift)
  test-linux-x64-experimental:
    name: Linux x64 - Experimental
    uses: ./.github/workflows/_build-test-reusable.yml
    with:
      runner: ubuntu-latest
      triplet: x64-linux
      config: experimental
      build-type: Release
      cache-key: ci-matrix-linux-x64-exp

  # Linux x64 - Dynamic build
  test-linux-x64-dynamic:
    name: Linux x64 - Dynamic
    uses: ./.github/workflows/_build-test-reusable.yml
    with:
      runner: ubuntu-latest
      triplet: x64-linux-dynamic
      config: production
      build-type: Release
      cache-key: ci-matrix-linux-x64-dyn

  # ==========================================================================
  # macOS Tests
  # ==========================================================================

  # macOS x64 - Production (static)
  test-macos-x64:
    name: macOS x64 - Production
    uses: ./.github/workflows/_build-test-reusable.yml
    with:
      runner: macos-13
      triplet: x64-osx
      config: production
      build-type: Release
      cache-key: ci-matrix-macos-x64

  # macOS ARM64 - Production (static)
  test-macos-arm64:
    name: macOS ARM64 - Production
    uses: ./.github/workflows/_build-test-reusable.yml
    with:
      runner: macos-latest
      triplet: arm64-osx
      config: production
      build-type: Release
      cache-key: ci-matrix-macos-arm64

  # macOS x64 - Dynamic build
  test-macos-x64-dynamic:
    name: macOS x64 - Dynamic
    uses: ./.github/workflows/_build-test-reusable.yml
    with:
      runner: macos-13
      triplet: x64-osx-dynamic
      config: production
      build-type: Release
      cache-key: ci-matrix-macos-x64-dyn

  # macOS ARM64 - Dynamic build
  test-macos-arm64-dynamic:
    name: macOS ARM64 - Dynamic
    uses: ./.github/workflows/_build-test-reusable.yml
    with:
      runner: macos-latest
      triplet: arm64-osx-dynamic
      config: production
      build-type: Release
      cache-key: ci-matrix-macos-arm64-dyn

  # ==========================================================================
  # Windows Tests
  # ==========================================================================

  # Windows x64 - Production (static)
  test-windows-x64:
    name: Windows x64 - Production
    uses: ./.github/workflows/_build-test-reusable.yml
    with:
      runner: windows-latest
      triplet: x64-windows-static
      config: production
      build-type: Release
      cache-key: ci-matrix-windows-x64

  # Windows ARM64 - Production (static)
  test-windows-arm64:
    name: Windows ARM64 - Production
    uses: ./.github/workflows/_build-test-reusable.yml
    with:
      runner: windows-latest
      triplet: arm64-windows-static
      config: production
      build-type: Release
      cache-key: ci-matrix-windows-arm64

  # Windows x64 - Dynamic build
  test-windows-x64-dynamic:
    name: Windows x64 - Dynamic
    uses: ./.github/workflows/_build-test-reusable.yml
    with:
      runner: windows-latest
      triplet: x64-windows-dynamic
      config: production
      build-type: Release
      cache-key: ci-matrix-windows-x64-dyn

  # Windows ARM64 - Dynamic build
  test-windows-arm64-dynamic:
    name: Windows ARM64 - Dynamic
    uses: ./.github/workflows/_build-test-reusable.yml
    with:
      runner: windows-latest
      triplet: arm64-windows-dynamic
      config: production
      build-type: Release
      cache-key: ci-matrix-windows-arm64-dyn

  # ==========================================================================
  # Summary Job
  # ==========================================================================

  summary:
    name: CI Matrix Summary
    needs:
      - test-linux-x64-production
      - test-linux-x64-experimental
      - test-linux-x64-dynamic
      - test-macos-x64
      - test-macos-arm64
      - test-macos-x64-dynamic
      - test-macos-arm64-dynamic
      - test-windows-x64
      - test-windows-arm64
      - test-windows-x64-dynamic
      - test-windows-arm64-dynamic
    runs-on: ubuntu-latest
    if: always()

    steps:
      - name: Generate summary
        run: |
          echo "=== CI Matrix Workflow Summary ==="
          echo ""
          echo "Date: $(date -u +"%Y-%m-%d %H:%M:%S UTC")"
          echo "Run: ${{ github.run_number }}"
          echo "Commit: ${{ github.sha }}"
          echo "Branch: ${{ github.ref_name }}"
          echo ""
          echo "Linux:"
          echo "  x64 Production: ${{ needs.test-linux-x64-production.result }}"
          echo "  x64 Experimental: ${{ needs.test-linux-x64-experimental.result }}"
          echo "  x64 Dynamic: ${{ needs.test-linux-x64-dynamic.result }}"
          echo ""
          echo "macOS:"
          echo "  x64 Production: ${{ needs.test-macos-x64.result }}"
          echo "  ARM64 Production: ${{ needs.test-macos-arm64.result }}"
          echo "  x64 Dynamic: ${{ needs.test-macos-x64-dynamic.result }}"
          echo "  ARM64 Dynamic: ${{ needs.test-macos-arm64-dynamic.result }}"
          echo ""
          echo "Windows:"
          echo "  x64 Production: ${{ needs.test-windows-x64.result }}"
          echo "  ARM64 Production: ${{ needs.test-windows-arm64.result }}"
          echo "  x64 Dynamic: ${{ needs.test-windows-x64-dynamic.result }}"
          echo "  ARM64 Dynamic: ${{ needs.test-windows-arm64-dynamic.result }}"
          echo ""

          # Count passed/failed
          JOBS=(
            "${{ needs.test-linux-x64-production.result }}"
            "${{ needs.test-linux-x64-experimental.result }}"
            "${{ needs.test-linux-x64-dynamic.result }}"
            "${{ needs.test-macos-x64.result }}"
            "${{ needs.test-macos-arm64.result }}"
            "${{ needs.test-macos-x64-dynamic.result }}"
            "${{ needs.test-macos-arm64-dynamic.result }}"
            "${{ needs.test-windows-x64.result }}"
            "${{ needs.test-windows-arm64.result }}"
            "${{ needs.test-windows-x64-dynamic.result }}"
            "${{ needs.test-windows-arm64-dynamic.result }}"
          )

          PASSED=0
          FAILED=0
          SKIPPED=0

          for job in "${JOBS[@]}"; do
            if [[ "$job" == "success" ]]; then
              ((PASSED++))
            elif [[ "$job" == "failure" ]]; then
              ((FAILED++))
            elif [[ "$job" == "skipped" ]]; then
              ((SKIPPED++))
            fi
          done

          echo "Results: $PASSED passed, $FAILED failed, $SKIPPED skipped"
          echo ""

          if [[ $FAILED -gt 0 ]]; then
            echo "::error::$FAILED job(s) failed"
            exit 1
          fi

      - name: Create summary report
        if: always()
        run: |
          echo "## CI Matrix Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Linux" >> $GITHUB_STEP_SUMMARY
          echo "| Triplet | Configuration | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|---------|---------------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| x64-linux | Production | ${{ needs.test-linux-x64-production.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| x64-linux | Experimental | ${{ needs.test-linux-x64-experimental.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| x64-linux-dynamic | Production | ${{ needs.test-linux-x64-dynamic.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### macOS" >> $GITHUB_STEP_SUMMARY
          echo "| Triplet | Configuration | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|---------|---------------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| x64-osx | Production | ${{ needs.test-macos-x64.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| arm64-osx | Production | ${{ needs.test-macos-arm64.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| x64-osx-dynamic | Production | ${{ needs.test-macos-x64-dynamic.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| arm64-osx-dynamic | Production | ${{ needs.test-macos-arm64-dynamic.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Windows" >> $GITHUB_STEP_SUMMARY
          echo "| Triplet | Configuration | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|---------|---------------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| x64-windows-static | Production | ${{ needs.test-windows-x64.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| arm64-windows-static | Production | ${{ needs.test-windows-arm64.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| x64-windows-dynamic | Production | ${{ needs.test-windows-x64-dynamic.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| arm64-windows-dynamic | Production | ${{ needs.test-windows-arm64-dynamic.result }} |" >> $GITHUB_STEP_SUMMARY
