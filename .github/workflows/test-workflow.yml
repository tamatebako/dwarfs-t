name: Test Workflow

on:
  push:
    branches: [feature/multi-format-serialization-fuse]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: echo "Workflow test successful"
