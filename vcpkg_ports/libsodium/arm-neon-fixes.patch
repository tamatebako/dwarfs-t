diff --git a/src/libsodium/crypto_ipcrypt/ipcrypt_armcrypto.c b/src/libsodium/crypto_ipcrypt/ipcrypt_armcrypto.c
index 1234567..abcdefg 100644
--- a/src/libsodium/crypto_ipcrypt/ipcrypt_armcrypto.c
+++ b/src/libsodium/crypto_ipcrypt/ipcrypt_armcrypto.c
@@ -23,7 +23,11 @@

 #include "ipcrypt_ipsrvp.h"

+#if defined(__GNUC__) && (__GNUC__ >= 13)
+/* GCC 13+ has stricter type checking for NEON intrinsics */
+#pragma GCC diagnostic push
+#pragma GCC diagnostic ignored "-Wvector-conversion"
+#endif
+
 #ifdef __ARM_FEATURE_CRYPTO
 #ifdef __aarch64__

@@ -330,4 +334,8 @@ vec_to_u64x2_fast(const uint8x16_t v) {
     return result;
 }

+#if defined(__GNUC__) && (__GNUC__ >= 13)
+#pragma GCC diagnostic pop
+#endif
+
 #else /* __aarch64__ */

 #if defined(__ARM_NEON) && defined(__ARM_FEATURE_CRC32)
